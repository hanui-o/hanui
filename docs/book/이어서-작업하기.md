# 이어서 작업하기 (핸드오프)

> VSCode의 Claude Code(또는 새 세션)에서 이 책 작업을 이어갈 때 이 파일부터 읽으면 된다.
> 작성 환경 조건: **Claude Code**여야 함 (CLAUDE.md·`/blog` 커맨드·프로젝트 메모리를 읽는 도구). Cursor·일반 챗봇은 맥락이 안 따라옴.

## 프로젝트 한 줄

책 **「퇴근 30분 빨라지는 AI」**(가제)를 블로그 글로 매일 한 꼭지씩 쓰며 초고를 만든다.
하루 한 꼭지 = 책 한 꼭지.

## 먼저 읽을 문서 (이 순서)

1. `docs/book/기획서.md` — 타겟·컨셉·차별점·목차·리스크 (마스터)
2. `docs/blog/content-plan.md` — 목차 = 주제 큐 + 이번 주 일정 + 글 상태
3. `.claude/commands/blog.md` — `/blog` 커맨드(글 작성 규칙·독자 모드)
4. 프로젝트 메모리는 Claude Code가 자동 로드 (`book-ai-not-scary`)

## 핵심 결정 (요약)

- **타겟:** 글은 경리에게, 책은 사무직 전체에 판다.
- **컨셉:** 코딩 없이 AI한테 말 걸어 **업무 도구를 직접 만든다**(물어보기→만들기). 파는 건 버튼이 아니라 **판단**.
- **도구:** 클로드 Artifacts(브라우저). "코딩" 단어 안 씀.
- **톤:** 쉬운 말·비유. 글 끝마다 "오늘 딱 하나" + "이 장의 판단 1가지".
- **금지:** 주식·투자 등 금융 조언성 콘텐츠.

## 자동 발행 시스템 (중요)

**글은 시간 날 때 몰아서 쓰고, 발행은 매일 자동.** 직접 발행 버튼 누를 필요 없다.

- **예약 큐:** `apps/docs/blog-queue/` 에 mdx를 두면 사이트에 안 보인다(대기).
- **자동 발행:** 매일 09:10 KST, GitHub Actions(`.github/workflows/daily-publish.yml`)가 큐에서
  `date`가 오늘 이하인 글을 `apps/docs/src/content/blog/`로 옮겨 커밋·푸시 → Vercel 자동 배포.
- **재고 알림:** 남은 예약글 < 3편이면 GitHub 이슈(`blog-restock` 라벨) 자동 생성.
- **쟁여두는 법:** 큐에 글 작성 + `date`를 미래로(하루 간격) + 커밋·푸시. 끝. 자세히는 `apps/docs/blog-queue/README.md`.
- **로컬 점검:** `DRY_RUN=1 node scripts/publish-due-posts.mjs`

## 진행 상황 (2026-07-14 기준) — 책 13장 초고 전부 작성 완료 🎉

책 시리즈 `퇴근 30분 빨라지는 AI` 13장 초고를 다 썼다. 6~8장 발행됨, 9~13장은 큐에서 자동 발행 예정.

**발행됨** (`apps/docs/src/content/blog/`):

- order 1 `ai-is-your-intern` / 2 `how-to-ask-ai` / 3 `ask-vs-build` / 4 `when-to-trust-ai` (1부)
- order 5 `email-tool-with-ai` (2부 샘플) / 6 `excel-helper-tool` / 7 `receipt-organizer-tool` / 8 `summarize-tool`

**예약 큐 대기** (`apps/docs/blog-queue/`, 발행일에 자동 발행):

- order 9 `report-draft-tool` — 07-15
- order 10 `notice-form-tool` — 07-16 (2부 끝)
- order 11 `fixing-ai-output` — 07-17
- order 12 `share-with-team` — 07-18
- order 13 `next-step-tools` — 07-19 (책 마지막 장)

## 다음 할 일 (초고 다음 단계)

- 13장 다 나가면 큐가 빈다. 이후엔 **초고 → 퇴고**로 넘어갈 것: 전체 흐름 점검, 중복 정리, 책 제목 확정 후 시리즈명 일괄 변경.
- 확장 아이디어(파포/지라 등)는 1차 제외였음. 필요 시 기획서 참고.
- 자동 발행이 멈춘 적 있음: **큐가 마르면 발행이 선다** (06-29~07-07, 07-09~07-14 공백이 그 사례). 초고 단계 끝났으니 이제 이 문제는 자연히 없어짐.

## 미정 / 정리할 것

- 탐색용 글 3편(책 밖) — 유지/삭제 결정 필요:
  - `apps/docs/src/content/blog/ai-is-like-an-intern.mdx` (구버전 인턴, 새 1장이 상위 호환 → 삭제 추천)
  - `apps/docs/src/content/blog/make-family-invite.mdx` (4050 가족 초대장)
  - `apps/docs/src/content/blog/why-claude-stayed.mdx` (개발자용)
- 아직 커밋 안 함.
- 책 제목 확정 시 시리즈명 일괄 변경.

## 로컬 미리보기

```bash
cd apps/docs && pnpm dev   # http://localhost:3000/blog/<slug>
```
