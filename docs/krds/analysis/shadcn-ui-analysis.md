# shadcn/ui 문서 사이트 분석

> 분석 날짜: 2025-11-12
> 분석 대상: https://ui.shadcn.com/docs/components/button

## 목차

1. [페이지 구조](#페이지-구조)
2. [핵심 기능](#핵심-기능)
3. [구현 우선순위](#구현-우선순위)
4. [기술 스택 추정](#기술-스택-추정)

---

## 페이지 구조

### 레이아웃 (3단 구조)

```
┌─────────────┬──────────────────────┬─────────────┐
│             │                      │             │
│  Left       │   Main Content       │  Right      │
│  Sidebar    │                      │  TOC        │
│             │                      │             │
│  - Sections │   - Component Demo   │  On This    │
│  - Get      │   - Code Blocks      │  Page:      │
│    Started  │   - Installation     │             │
│  - Compo-   │   - Usage            │  - Install  │
│    nents    │   - Examples         │  - Usage    │
│  - Forms    │                      │  - Cursor   │
│  - etc.     │                      │  - Examples │
│             │                      │  - API Ref  │
└─────────────┴──────────────────────┴─────────────┘
```

### 헤더 요소

- **Copy Page** 버튼 (페이지 전체 복사)
- **이전/다음 페이지** 네비게이션 버튼
- **업데이트 공지 배너** (새로운 기능 안내)

---

## 핵심 기능

### 1. 코드 블록 시스템 ⭐⭐⭐

**가장 중요한 기능**

#### 기능 상세

##### a) 라인 넘버링

```tsx
  1  import { Button } from "@/components/ui/button"
  2
  3  export function ButtonDemo() {
  4    return <Button>Click me</Button>
  5  }
```

- 회색 텍스트
- 왼쪽 정렬
- 읽기 쉬운 간격

##### b) 문법 하이라이팅 (Syntax Highlighting)

**색상 구분:**

- **키워드**: `import`, `export`, `function`, `return` → 빨간색/보라색
- **컴포넌트/타입**: `Button`, `ButtonDemo` → 파란색
- **문자열**: `"@/components/ui/button"` → 초록색
- **속성/Props**: `variant`, `size` → 주황색
- **주석**: `// comment` → 회색

##### c) 복사 버튼

- 위치: 코드 블록 오른쪽 상단
- 아이콘: 클립보드
- 인터랙션: 호버 시 강조, 클릭 시 피드백

##### d) 파일명 표시

```
┌─────────────────────────┐
│ 📄 globals.css          │ ← 파일 아이콘 + 파일명
├─────────────────────────┤
│   1  @layer base {      │
│   2    button {...}     │
└─────────────────────────┘
```

#### 코드 블록 종류

**Type 1: 전체 컴포넌트 코드**

```tsx
import { ArrowUpIcon } from 'lucide-react';
import { Button } from '@/components/ui/button';

export function ButtonDemo() {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Button variant="outline">Button</Button>
    </div>
  );
}
```

- Import 문 포함
- 전체 함수 구조
- 컨텍스트 제공

**Type 2: 간단한 사용 코드**

```tsx
<Button variant="outline">Button</Button>
```

- 핵심 사용법만
- 빠른 복사용
- 배경색 약간 다름

---

### 2. 컴포넌트 프리뷰 영역 ⭐⭐

#### 특징

- **실제 작동하는 컴포넌트** 표시
- **인터랙티브**: 클릭, 호버 등 실제 동작
- **배경**: 흰색/회색 구분된 영역
- **테두리**: subtle border로 구분

#### 예시 구조

```
┌──────────────────────────────────┐
│                                  │
│   [Button]  [↑]                 │  ← 실제 버튼들
│                                  │
└──────────────────────────────────┘
     1  import { Button } from ...
     2  export function Demo() {...
```

---

### 3. Installation 섹션 ⭐⭐

#### UI 구조

```
Installation

┌─────┬────────┐
│ CLI │ Manual │  ← 탭
└─────┴────────┘

┌──────┬─────┬──────┬─────┐
│ pnpm │ npm │ yarn │ bun │  ← 패키지 매니저 선택
└──────┴─────┴──────┴─────┘

$ pnpm dlx shadcn@latest add button  [복사]
```

#### 기능

1. **탭 전환**: CLI ↔ Manual
2. **패키지 매니저 선택**
   - pnpm (default)
   - npm
   - yarn
   - bun
3. **설치 명령어**
   - 선택한 패키지 매니저에 맞게 자동 변경
   - 복사 버튼

---

### 4. 목차 네비게이션 (On This Page) ⭐

#### 위치 및 구조

**오른쪽 사이드바**

```
On This Page
├─ Installation
├─ Usage
├─ Cursor
├─ Examples
│  ├─ Size
│  ├─ Default
│  ├─ Outline
│  ├─ Secondary
│  ├─ Ghost
│  ├─ Destructive
│  ├─ Link
│  ├─ Icon
│  ├─ With Icon
│  ├─ Rounded
│  ├─ Spinner
│  ├─ Button Group
│  └─ Link
└─ API Reference
```

#### 기능

- **스크롤 스파이**: 현재 위치 강조
- **부드러운 스크롤**: 클릭 시 애니메이션
- **계층 구조**: 중첩된 섹션 표시

---

### 5. Examples 섹션 구조 ⭐⭐

#### 각 Example 반복 패턴

```
┌─────────────────────────────────────┐
│ ## Size                             │  ← 제목
│                                     │
│ ┌─────────────────────────────┐   │
│ │                             │   │
│ │  [Small] [Default] [Large]  │   │  ← 프리뷰
│ │                             │   │
│ └─────────────────────────────┘   │
│                                     │
│ ┌─────────────────────────────┐   │
│ │  1  import { Button } ...   │   │  ← 전체 코드
│ │  2                          │   │
│ │  3  export function...      │   │
│ └─────────────────────────────┘   │
│                                     │
│ ┌─────────────────────────────┐   │
│ │ // Small                    │   │
│ │ <Button size="sm">...</>    │   │  ← 간단한 코드
│ │                             │   │
│ │ // Medium                   │   │
│ │ <Button>...</Button>        │   │
│ └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### Example 종류

1. **Size** - 크기 variants (sm, md, lg)
2. **Default** - 기본 스타일
3. **Outline** - 테두리만
4. **Secondary** - 보조 스타일
5. **Ghost** - 투명 배경
6. **Destructive** - 위험/삭제 액션
7. **Link** - 링크 스타일
8. **Icon** - 아이콘만
9. **With Icon** - 텍스트 + 아이콘
10. **Rounded** - 완전히 둥근 모서리
11. **Spinner** - 로딩 상태
12. **Button Group** - 버튼 그룹

---

### 6. Usage 섹션

#### 구조

```tsx
import { Button } from '@/components/ui/button';
```

```tsx
<Button variant="outline">Button</Button>
```

- Import 문
- 기본 사용 예제

---

### 7. Cursor 섹션

#### 내용

- Tailwind v4 변경사항 안내
- CSS 코드로 cursor 설정 방법
- 파일명 표시 (globals.css)

```css
@layer base {
  button:not(:disabled) {
    cursor: pointer;
  }
}
```

---

## 구현 우선순위

### Phase 1: 필수 기능 (1-2주)

#### 1. 코드 블록 컴포넌트

- [ ] 라인 넘버링
- [ ] 복사 버튼
- [ ] 문법 하이라이팅 (syntax highlighter 라이브러리)
- [ ] 파일명 표시

**추천 라이브러리:**

- `react-syntax-highlighter`
- `prism-react-renderer`
- `shiki` (Next.js 권장)

#### 2. 컴포넌트 프리뷰 영역

- [ ] 독립된 렌더링 영역
- [ ] 테두리/배경 스타일

#### 3. 기본 페이지 레이아웃

- [ ] 3단 레이아웃 구조
- [ ] 헤더
- [ ] 메인 컨텐츠 영역

---

### Phase 2: 핵심 UX (2-3주)

#### 4. Installation 섹션

- [ ] CLI/Manual 탭
- [ ] 패키지 매니저 선택 (pnpm/npm)
- [ ] 동적 명령어 생성

#### 5. 목차 네비게이션

- [ ] 오른쪽 TOC 사이드바
- [ ] 스크롤 스파이 기능
- [ ] 부드러운 스크롤

#### 6. Examples 섹션 구조

- [ ] 섹션별 프리뷰 + 코드 패턴
- [ ] 모든 variant 예제

---

### Phase 3: 추가 기능 (1주)

#### 7. 페이지 네비게이션

- [ ] Copy Page 버튼
- [ ] 이전/다음 버튼
- [ ] 업데이트 공지 배너

#### 8. 반응형 디자인

- [ ] 모바일 레이아웃
- [ ] 사이드바 토글

---

## 기술 스택 추정

### shadcn/ui 사용 기술

#### 프레임워크

- **Next.js** (App Router 추정)
- **React**
- **TypeScript**

#### 스타일링

- **Tailwind CSS**
- **CVA** (class-variance-authority)

#### 코드 하이라이팅

- **Shiki** 또는 **Rehype Pretty Code** (추정)

#### MDX

- **next-mdx-remote** 또는 **Contentlayer**
- 문서를 MDX로 작성

#### 컴포넌트

- **Radix UI** (headless components)
- **lucide-react** (아이콘)

---

## HANUI에 적용할 기술 스택

### 확정

- Next.js 15 (App Router)
- React 19
- TypeScript
- Tailwind CSS

### 추가 필요

- [ ] **MDX** 지원
  - `@next/mdx`
  - `next-mdx-remote`
- [ ] **코드 하이라이팅**
  - `shiki`
  - `rehype-pretty-code`
- [ ] **복사 기능**
  - `react-copy-to-clipboard`
  - 또는 Clipboard API 직접 사용

---

## 구현 전략

### 1단계: 컴포넌트 개발

```
/components
  /docs
    /CodeBlock
      - CodeBlock.tsx       (메인 컴포넌트)
      - LineNumbers.tsx     (라인 넘버)
      - CopyButton.tsx      (복사 버튼)
    /ComponentPreview
      - Preview.tsx
    /Installation
      - InstallTabs.tsx
      - PackageManager.tsx
    /TableOfContents
      - TOC.tsx
      - ScrollSpy.tsx
```

### 2단계: 문서 구조

```
/app/components
  /[component]
    - page.tsx          (동적 라우트)

/docs
  /button.mdx
  /input.mdx
  ...
```

### 3단계: 유틸리티

```
/lib
  /docs
    - code-highlighter.ts
    - mdx-processor.ts
    - toc-generator.ts
```

---

## 참고 자료

- shadcn/ui 공식 문서: https://ui.shadcn.com
- shadcn/ui GitHub: https://github.com/shadcn-ui/ui
- Tailwind CSS 문서: https://tailwindcss.com
- Shiki 문서: https://shiki.style
- Rehype Pretty Code: https://rehype-pretty-code.netlify.app

---

## 다음 단계

1. **CodeBlock 컴포넌트 구현** (최우선)
   - shiki 설치 및 설정
   - 라인 넘버링
   - 복사 기능

2. **MDX 설정**
   - @next/mdx 설치
   - mdx-components.tsx 작성

3. **Button 문서 페이지 작성**
   - /app/components/button/page.tsx
   - 모든 섹션 구현

4. **재사용 가능한 문서 템플릿 생성**
   - 다른 컴포넌트 문서에도 적용 가능한 구조
