---
title: '백엔드 잘 모르는 프론트가 Spring Boot API 짠 이야기'
description: '전자정부프레임워크가 뭔지도 몰랐던 프론트엔드 개발자가 Claude Code로 Spring Boot 백엔드를 짜게 된 과정. eGovFrame v5 블록 조합 + Claude Code 스터디 모드로 풀스택 구현.'
date: '2026-03-17'
tags: ['Claude Code', 'AI', 'Spring Boot', '백엔드', '전자정부프레임워크', '자동화', '개발도구']
author: 'odada'
series: 'Claude Code'
seriesOrder: 6
---

Claude Code 시리즈 6편이에요. [5편에서 MCP로 노션·피그마를 연동](/blog/claude-code-mcp)했는데요.

오늘은 프론트엔드 개발자가 백엔드까지 혼자 구현한 이야기예요.

저 **백엔드 잘 몰라요.** 프론트엔드 개발자거든요. Spring이라고 하면 어노테이션 몇 개 아는 수준이고, MyBatis는 이름만 들어봤고, JWT 구현은 블로그 긁어서 붙여넣는 사람이에요.

근데 지금 hanui CMS 백엔드를 짜고 있어요. Spring Boot + 전자정부프레임워크 v5로.

생존 코딩같은 거지만, 그래도 꽤 괜찮은 결과물이 나오고 있어요.

## 전자정부프레임워크 v5가 뭐예요?

공공기관 프로젝트 들어가면 꼭 나오는 그 이름. 행정안전부에서 만든 Java 기반 프레임워크예요.

쉽게 말하면 **공공기관 납품용 백엔드 블록 모음**이에요.

```
eGovFramework v5 = Spring Boot 3.x 기반
                 + 인증/세션 처리 블록
                 + 파일 업로드 블록
                 + 공통 코드 관리 블록
                 + 게시판 블록
                 + 로그/감사 블록
                 + 배치 처리 블록
                 + ...
```

HANUI가 UI 컴포넌트 블록 모음인 것처럼, 전자정부프레임워크는 **백엔드 기능 블록 모음**이에요.

### v5의 강점

v4까지는 Spring 구버전 기반이었는데, v5에서 **Spring Boot 3.x + Java 17**로 올라왔어요.

**보안** 측면에서 특히 강해요:

- 행안부에서 직접 검증한 보안 모듈 내장
- XSS, SQL Injection 방어 필터 기본 탑재
- 암호화 유틸 (AES, SHA 등) 기본 제공
- 감사 로그(Audit Log) 블록 — 누가 언제 뭘 했는지 자동 기록
- 취약점 업데이트 공식 지원

공공기관 입찰할 때 "전자정부프레임워크 적용"이 거의 필수 요건이에요. 쓸 수밖에 없기도 하지만, 막상 써보니까 **CMS에 필요한 기능들이 거의 다 블록으로 있어요.** 처음부터 만들 필요가 없어요.

## 블록 조합으로 CMS 구현

프론트에서 컴포넌트 조합하는 것처럼, 백엔드도 결국 블록 조합이에요.

hanui CMS 백엔드 스택:

```
전자정부프레임워크 v5 블록
  + Spring Security (RBAC 인증/권한)
  + MyBatis (DB 쿼리)
  + PostgreSQL (DB)
  + Flyway (DB 마이그레이션)
  + JWT (토큰 기반 인증)
  + Claude Code (조합 + 구현)
```

게시판, 파일 업로드, 공통 코드 관리는 eGovFrame 블록 그대로 가져다 썼어요. 직접 짤 필요가 없어요.

## 실제로 이렇게 했어요

### 1. 노션 기획서 → DB 스키마

노션에 DB 설계가 다 되어있었어요. 테이블, 컬럼, 관계 전부요.

```
나: 노션 기획서 DB 설계 페이지 보고
    PostgreSQL DDL 만들어줘
    Flyway 마이그레이션 파일로

Claude: (노션 직접 읽음)
        → V1__init_schema.sql 생성
```

저는 SQL을 잘 몰라도 기획서만 써놓으면 됐어요.

### 2. API 명세 → 코드

API 명세도 노션에 있었어요.

```
나: 게시글 CRUD API 만들어줘
    명세는 노션에 있어

Claude: PostController.java
        PostService.java
        PostMapper.java + PostMapper.xml
        PostDto.java (Request/Response)
        → 전부 생성
```

### 3. 모르는 거 나오면 바로 스터디

이게 진짜 좋았어요. 모르는 코드가 나오면 바로 물어봤어요.

```
나: @PreAuthorize("hasRole('EDITOR')") 이게 뭐야?

Claude: Spring Security에서 이 API를
        EDITOR 역할을 가진 사람만 접근할 수 있게 막는 거예요.
        RBAC(역할 기반 접근 제어)라고 해요.

나: 아 그럼 VIEWER는 이 API 못 쓰겠네?

Claude: 맞아요. VIEWER는 읽기만 되고 쓰기는 안 돼요.
```

```
나: @Transactional이 여기 왜 붙어있어?

Claude: 게시글 저장하고 첨부파일도 같이 저장하는데,
        둘 중 하나 실패하면 둘 다 취소해야 하거든요.
        그게 트랜잭션이에요.

나: 아 그래서 같이 묶는 거구나
```

Stack Overflow 긁어다 붙여넣는 것보다 이해가 훨씬 잘 돼요. 단순히 구현만 하는 게 아니라 **왜 이렇게 되는지**를 대화하면서 배우니까요.

이런 식으로 하다 보니까 Spring 흐름을 알게 됐어요. 

전자정부프레임워크 v5는 필요한 블록이 다 있어요. 보안, 인증, 게시판, 파일 업로드 — 공공 CMS에 필요한 것들이 이미 검증된 형태로 들어있어요. 저는 어떤 블록을 어디에 붙일지 **설계**만 하고, 구현은 Claude Code랑 같이 했어요.

---

[claude-settings](https://github.com/hanui-o/claude-settings)에 백엔드 커맨드도 있어요 (`/api`, `/db`, `/migrate`):

```bash
bash <(curl -sL https://raw.githubusercontent.com/hanui-o/claude-settings/main/setup.sh)
```

---

> **Claude Code 시리즈:**
>
> - [1편: CMS 프로젝트 하루만에 만들기](/blog/claude-code-cms-workflow)
> - [2편: 슬래시 커맨드 21종 설치](/blog/claude-code-settings)
> - [3편: CLAUDE.md 잘 쓰는 법](/blog/claude-code-claude-md)
> - [4편: 나만의 커맨드 만들기](/blog/claude-code-custom-commands)
> - [5편: MCP로 노션·피그마 연동](/blog/claude-code-mcp)
> - **6편: 백엔드 잘 모르는 프론트가 Spring Boot API 짠 이야기** ← 현재 글
> - 7편: 일주일 회고 (3/18)
>
> **관련 프로젝트:**
>
> - [claude-settings](https://github.com/hanui-o/claude-settings) — Claude Code 슬래시 커맨드, 에이전트, 훅 설정 모음
> - [HANUI](https://github.com/hanui-o/hanui) — KRDS 기반 React 디자인 시스템
